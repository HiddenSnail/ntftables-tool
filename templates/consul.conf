# Consul 模板
# server RPC(8300) + serf LAN(8301) + serf WAN(8302) + HTTP API(8500) + DNS(8600)
NAME="Consul"
DESCRIPTION="HashiCorp Consul service mesh (RPC/serf/HTTP/DNS)"
PORTS=("8300" "8301" "8302" "8500" "8600")
PROTOCOL="tcp"
